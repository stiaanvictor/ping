import { StyleSheet, Text, View } from "react-native";

function AdminUserCard() {
  return (
    <View style={styles.container}>
      <Text>Admin User Card</Text>
    </View>
  );
}

export default AdminUserCard;

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#EFEFEF",
  },
});
